# Django Docker Compose Project

Este proyecto es una plantilla para iniciar un proyecto de Django utilizando Docker Compose para la configuración del entorno de desarrollo.

## Requisitos previos
Tener Docker y Docker Compose instalados en tu sistema.


Ejecuta el siguiente comando para construir y levantar los contenedores de Docker:
docker-compose up -d --build

-Ingresar a la siguiente ruta: localhost:8000/
-Registrar usuario 
-Loguin

## Observaciones
No agregue los settings.py ni el archivo .env al archivo de gitignore solo por temas didacticos